name: macOS install test

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  install-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run installer
        run: |
          ./install.sh
